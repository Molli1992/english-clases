import styles from "./services.module.css";
import ServicesCard from "./servicesCard/servicesCard";

export default function Services() {
  return (
    <div className={`${styles.body} padding`}>
      <div className={`${styles.container} width`}></div>
    </div>
  );
}
